Installation
============

:code:`scenario_gym` can be installed with :code:`pip`:

.. code-block:: console

    pip install scenario-gym

To run tests or for development the :code:`dev` extra should be installed:

.. code-block:: console

    pip install scenario-gym[dev]
